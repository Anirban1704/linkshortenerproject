# UI Standards

All UI elements in this project must use shadcn UI components. Do not create custom UI components for this application.

## Required Rules
- Use shadcn UI components for all interface elements.
- Prefer existing shadcn patterns and primitives before introducing anything new.
- Keep styling consistent with the current Tailwind-based setup.
- Avoid building one-off custom components when a shadcn equivalent is available.
